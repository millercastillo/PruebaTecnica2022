@page "/generos"
@inject IRepositorio repositorio

<MudAppBar Color="Color.Primary" Fixed="false">
    <MudText Align="Align.Center" Typo="Typo.h4">Géneros</MudText>
</MudAppBar>

<MudButton Variant="Variant.Filled" Color="Color.Info" StartIcon="@Icons.Filled.Add" Link="generos/crear" Size="Size.Medium">Agregar Género</MudButton>


@code {
    

    //private async Task BorrarGenero(Genero genero)
    //{
    //    var responseHttp = await repositorio.Delete($"api/generos/{genero.Id}");
    //    if (responseHttp.Error)
    //    {
    //       await mostrarMensajes.MostrarMensajeError(await responseHttp.GetBody());
    //    }
    //    else
    //    {
    //        await Cargar();
    //    }

    //}
}